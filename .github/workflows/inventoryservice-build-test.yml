name: InventoryService - Build & Test

on:
  pull_request:
    paths:
      - 'InventoryService/**'
      - '.github/workflows/inventoryservice-*.yml'
  push:
    branches: [ "develop", "feature/**" ]
    paths:
      - 'InventoryService/**'

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore eShop.sln

      - name: Build
        run: dotnet build eShop.sln --no-restore -c Release

      - name: Test
        run: dotnet test eShop.sln --no-build -c Release --logger "trx;LogFileName=inventoryservice-tests.trx"

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: inventoryservice-test-results
          path: '**/*.trx'
