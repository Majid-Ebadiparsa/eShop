apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthmonitorservice
  namespace: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: healthmonitorservice
  template:
    metadata:
      labels:
        app: healthmonitorservice
        version: v1
    spec:
      containers:
      - name: healthmonitorservice
        image: healthmonitorservice:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ConnectionStrings__HealthMonitorDb
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: HEALTHMONITORDB_CONNECTION
        - name: ConnectionStrings__Redis
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: REDIS_CONNECTION
        - name: RabbitMQ__Host
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: RABBITMQ_HOST
        - name: RabbitMQ__Username
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: RABBITMQ_USERNAME
        - name: RabbitMQ__Password
          valueFrom:
            secretKeyRef:
              name: eshop-secrets
              key: RABBITMQ_PASSWORD
        - name: Consul__Host
          valueFrom:
            configMapKeyRef:
              name: eshop-config
              key: CONSUL_HOST
        - name: ServiceConfig__ServiceName
          value: "healthmonitorservice"
        - name: ServiceConfig__ServicePort
          value: "8080"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: healthmonitorservice
  namespace: eshop
  labels:
    app: healthmonitorservice
spec:
  selector:
    app: healthmonitorservice
  ports:
  - port: 80
    targetPort: 8080
    name: http
  type: ClusterIP

