name: OrderService - Build & Test

on:
  pull_request:
    paths:
      - 'OrderService/**'
      - '.github/workflows/orderservice-*.yml'
  push:
    branches: [ "develop", "feature/**" ]
    paths:
      - 'OrderService/**'

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore eShop.sln

      - name: Build
        run: dotnet build eShop.sln --no-restore -c Release

      - name: Test
        run: dotnet test eShop.sln --no-build -c Release --logger "trx;LogFileName=orderservice-tests.trx"

      - name: Publish test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orderservice-test-results
          path: '**/*.trx'
